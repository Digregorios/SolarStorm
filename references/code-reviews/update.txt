Phase 5 patch-forward status (current):

Shipped:
- B1: forecast --model auto --serve-residuals with per-CP router and NWP causality.
- CP20-22 residual serving behind flag; CP23 remains Ridge.
- Fallback to Ridge when no causal NWP available.
- Full routing telemetry in JSON output.
- CLI end-to-end tests for ECMWF residual, GFS residual, fallback Ridge, CP23 Ridge.
- Leakage test: no residual served with run_time_utc > cp_utc - safety_margin.

Patch-forward (this pass):
- [P2] Cache repair now validates freshly downloaded snapshot with select_nwp_v1 before
  breaking the cycle loop; if t2m_c is unusable for CP, continues to older cycles.
- [P2] MOS/EMOS-lite evaluator now serializes min_coverage_required (0.70) and
  coverage_guard in gate_contract; reports regenerated with engaged_n/coverage fields.
- [P3] Telemetry expanded: candidate_run_times (all evaluated), http_attempted_run_times
  (actually fetched over HTTP), selected_run_time (cycle used for serving).
- [P3] Cache validation errors now use logging.debug instead of traceback.print_exc.

Deferred:
- B2: offline backtest report (serving path, fallback rate, calm guard, leakage gate).
- Shadow Ops v1: 7-14 day live forecast run with fetch_success_rate, cache_repair_count,
  fallback_rate, residual_served_rate, run_age_h, valid_time_delta_h, json_completeness.