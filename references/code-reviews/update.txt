Escolha **2. B1 first**.

É o passo certo para manter velocidade sem transformar Track B num mega-commit leakage-sensitive. B1 entrega o caminho operacional real, mas atrás de `--serve-residuals`, então não muda o default nem declara promoção.

Escopo desta passada:

- `forecast --model auto --serve-residuals`.
- Só CP20-22 podem servir residual; CP23 continua Ridge.
- Se não houver NWP causal, fallback determinístico para Ridge.
- Telemetria completa no JSON: `model_route`, `served_model`, endpoints, `run_time_utc`, `valid_time_utc`, `valid_time_delta_h`, `lead_h`, `run_age_h`, `fallback_reason`.
- Testes CLI end-to-end para ECMWF residual, GFS residual, fallback Ridge, CP23 Ridge.
- Leakage test explícito: nenhum residual servido com `run_time_utc > cp_utc - 60min`.

Não marcar “Track B completo” ainda. O commit deve dizer claramente: **B1 shipped; B2 deferred**.

B2 vem logo depois como relatório offline: backtest do caminho exato de serving, fallback rate real, calm guard, leakage gate e evidência para o reviewer. Sem B2, o path pode existir atrás da flag, mas não deve ser promovido nem usado como prova de readiness.