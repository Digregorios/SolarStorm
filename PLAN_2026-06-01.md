# Plano de Implementação — Baseline SolarStorm

**Data:** 2026-06-01
**Status:** Ativo
**Foco:** Implementar o plano integralmente como baseline ANTES de qualquer tentativa de melhoria
**Fonte do plano:** Conversa Hermes ↔ Alan (2026-06-01)
**Cross-ref:** `references/code-reviews/update.txt` (reviewer P1 resolvido; higiene P2 aplicada 2026-06-01)

---

## Visão Geral

5 fases, ordem importa:
1. **Fase 1** — T-11-6: Spread GFS-ECMWF (EXECUTADO; fixes P1/P2 do update.txt APLICADOS)
2. **Fase 2** — Matriz Consolidada de Preditor (EXECUTADA; reports gerados + patch leak-free P1 + higiene P2)
3. **Fase 3** — Serving Mínimo (`--model auto` + fix pyproject.toml) — **EXECUTADA (2026-06-02)**
4. **Fase 4** — CQR/LightGBM Quantile (gated, após 1+2)
5. **Fase 5** — Live NWP (após preditor escolhido)

**Princípio:** ECMWF não deve ser descartado pelo KILL do T-11-5. Falhou auto-promote de ponto, mas mostrou sinal forte em CP20-22. Rota certa = spread + decisão condicional, não vencedor global.

---

## Fase 1 — T-11-6: Spread GFS-ECMWF

**Status:** EXECUTADO (2026-06-01); FIXES P1/P2 do update.txt APLICADOS (commit d489f27)
**Verdict:** FEASIBLE-CONDITIONAL (seasonal flip detectado em CP20-21)

### Entregáveis (já existem)
- ✅ `contracts/two_model_spread_feasibility_v0_prereg.md` (v1.0)
- ✅ `scripts/evaluate_two_model_spread_feasibility.py`
- ✅ `reports/calibration/two_model_spread_feasibility.md` (corrigido 2026-06-01)
- ✅ `reports/calibration/two_model_spread_feasibility.json` (corrigido 2026-06-01)
- ✅ `reports/calibration/two_model_spread_feasibility_review.md` (corrigido 2026-06-01)
- ✅ `reports/calibration/two_model_spread_diagnosis.md`

### Gate (já atendido com ressalva)
- ✅ Spearman(spread, abs_error) positivo em >= 2 folds (tenuous, seasonal flip)
- ✅ Q4 > Q1 (only in fold1 warm)
- ✅ Sinal por CP (concentrado em 20-22Z, condicional)
- ✅ Sem mexer em execução, odds ou calibração

### FIXES OBRIGATÓRIOS (do update.txt) — RESOLVIDOS (commit d489f27)

**P1 (críticos):**
1. **`two_model_spread_feasibility.md` linha 83** — diz "FEASIBLE + conditional point-routing signal" mas veredito é FEASIBLE-CONDITIONAL e diagnóstico diz "NOT for point routing". Origem: `evaluate_two_model_spread_feasibility.py:515` usa `if feasible:` em vez de ramificar pelo verdict.
2. **`evaluate_two_model_spread_feasibility.py:393 → :422`** — `cp_sign_consistent` e `sign_reversal` são calculados mas não entram no `report_json`. Automação vê só verdict final, sem saber que CP20/CP21 inverteram sinal.

**P2 (importantes):**
3. **`two_model_spread_feasibility_review.md:90`** — ainda fala "FEASIBLE is technically correct" (desatualizado).
4. **Prereg menciona `spread de max-traj`** mas avaliador só usa `spread_at_cp`. `nwp_t2m_maxtraj_spread_c` NÃO está validado. Deixar claro no relatório final.
5. **`tasks.md:845`** — diz "spread se T-11-6 GO", precisa virar "spread SÓ como feature candidata, com interação estação/regime e ablação obrigatória".

### Agentes em paralelo
- **Agente A:** corrigir renderer em `evaluate_two_model_spread_feasibility.py:515` + gravar `cp_sign_consistent`/`sign_reversal` no JSON. Re-rodar evaluator.
- **Agente B:** anti-leakage review das mudanças (mesmas linhas, run causal, thresholds train-only).
- **Agente C:** atualizar `two_model_spread_feasibility_review.md:90` + `tasks.md:845` para refletir FEASIBLE-CONDITIONAL.

### Critério de done
- [x] P1 #1 resolvido (renderer correto)
- [x] P1 #2 resolvido (JSON tem chaves de sign/reversal)
- [x] P2 #3 resolvido (review atualizado)
- [x] P2 #4 resolvido (escopo validado documentado)
- [x] P2 #5 resolvido (tasks.md atualizado)
- [x] Suite verde (367)
- [x] CHANGELOG atualizado

---

## Fase 2 — Matriz Consolidada de Preditor

**Status:** EXECUTADA (2026-06-01) — reports gerados; patch leak-free P1 + higiene P2 aplicados; rerun n_estimators=500 mantém o roteamento (CP20-22 ECMWF-residual; CP23 Ridge)
**Objetivo:** decidir melhor modelo por CP e regime, sem promover por um único relatório

### Candidatos (mesmas linhas, mesmos splits)
1. Ridge base (floor / CP23 incumbent)
2. GFS-residual (Phase-4 residual LGBM, GFS anchor)
3. ECMWF-residual (T-11-5 strong CP20-22 candidate)
4. Analog high-risk arm (T-9-1, GO)
5. GFS+ECMWF ensemble (SÓ se não regressar CP23, em rota condicional)

### Entregáveis
- ✅ `contracts/serving_candidate_matrix_v0_prereg.md` (v1.0) — já existe
- ✅ `scripts/evaluate_serving_candidate_matrix.py` — já existe
- ✅ `reports/serving/candidate_matrix_v0.md` — gerado
- ✅ `reports/serving/candidate_matrix_v0.json` — gerado
- ✅ `reports/serving/candidate_matrix_v0_review.md` — review PASS 10/10 (atualizado P2 2026-06-01)

### Gate
- Melhor MAE/RPS por CP
- Não piorar stable/calm days
- **Separar rigidamente CP20-22 de CP23**
- Se ECMWF só ajuda CP20-22, promover como candidato early-lead (não default global)

### Agentes
- **Agente A:** matriz all/CP (run do script)
- **Agente B:** matriz por regime (calm, non_calm, high_delta)
- **Agente C:** review de decisão (anti winner-shopping)

### Critério de done
- [x] Script executado sem erro
- [x] Report `.md` + `.json` gerados em `reports/serving/`
- [x] Winner-shopping guard explícito
- [x] Routing conservador recomendado (não auto-promote)
- [x] Suite verde
- [x] CHANGELOG atualizado

---

## Fase 3 — Serving Mínimo

**Status:** EXECUTADO (2026-06-02) — router conservador por-CP + `--model auto` + fallback gracioso; serving sem NWP vivo (isso é Fase 5)
**Objetivo:** tirar o projeto do default empirical, que hoje é gargalo prático

### Implementação
- Adicionar modo `--model auto` na CLI
- **Começar conservador:**
  - **CP23:** Ridge ou GFS-residual (conforme matriz)
  - **CP20-22:** NWP-residual (se matriz confirmar)
  - **Non-calm:** analog arm (se melhorar sem regressão)
  - **Intervalo:** ridge_conformal_minimal, com banner diagnóstico
- **Corrigir `pyproject.toml`:** declarar `lightgbm` e `httpx` que são usados mas não declarados

**Escopo entregue vs adiado (honesto):** o router implementa a tabela de PONTO por-CP da matriz
(CP20-22 -> ecmwf_residual, com fallback gfs_residual e depois ridge por disponibilidade; CP23 -> ridge
sempre, GFS nunca promovido pois `calm_ok=false`). Como o serving da Fase 3 NÃO tem NWP vivo (isso é a
Fase 5), `auto` resolve para ridge — ou degrada para o piso `empirical` se faltar dado de treino (<100
linhas) em vez de explodir. O roteamento por analog-arm em non-calm e o intervalo
`ridge_conformal_minimal` ficam ADIADOS (não são necessários para tirar o default do empirical); o banner
diagnóstico de routing FOI entregue. O spread `|GFS-ECMWF|` nunca é entrada de routing (invariante
testada). O router é geral: a Fase 5 só liga os flags `ecmwf_available/gfs_available` e a MESMA tabela
roteia para os residuais — sem mudar a lógica.

### Entregáveis
- ✅ `core/cli/forecast.py` — `auto` adicionado ao enum (default segue `empirical`, sem troca silenciosa)
- ✅ `core/cli/routing.py` — router conservador por-CP (tabela congelada da matriz) + `resolve_servable`
- ✅ `pyproject.toml` — `lightgbm>=4.0`, `httpx>=0.27` declarados (`pip install -e .` self-contained)
- ✅ `tests/unit/test_routing.py` — unit tests do router (tabela, invariantes, frozen)
- ✅ `tests/unit/test_cli_auto.py` — smoke `--model auto --dry-run` (JSON válido + degrade não explode)
- ✅ Docs: `docs/serving.md` (runbook) + CHANGELOG

### Agentes
- **Agente A:** CLI/model routing
- **Agente B:** testes unitários e smoke
- **Agente C:** docs e runbook

### Critério de done
- [x] `pyproject.toml` corrigido + `pip install -e .` funciona
- [x] `--model auto` implementado e testado
- [x] Routing conservador (não agressivo)
- [x] Banner diagnóstico aparece (stderr; stdout fica JSON puro)
- [x] Suite verde (387)
- [x] CHANGELOG atualizado
- [x] Runbook escrito (`docs/serving.md`)

---

## Fase 4 — CQR / LightGBM Quantile

**Status:** PREREG JÁ EXISTE (`contracts/cqr_lightgbm_quantile_v0_prereg.md`)
**Regra:** Rodar SÓ depois da Fase 1 e 2, para alimentar CQR com o melhor feature set

### Gate (permanece duro)
- Global IC80 em faixa
- REQ-AUD-5 het gate
- Sem inflar largura
- **Se falhar:** aceitar formalmente que IC80 inteiro 80% NÃO é recuperável com essa granularidade

### Entregáveis
- ✅ `contracts/cqr_lightgbm_quantile_v0_prereg.md` (v1.0) — já existe
- ⏳ `scripts/evaluate_cqr_lightgbm_quantile.py` (a criar)
- ⏳ `reports/calibration/cqr_lightgbm_quantile_v0.{md,json}` (a criar)

### Critério de done
- [ ] Alimentado pelo melhor feature set (Fase 1+2 outputs)
- [ ] CQR (LightGBM quantile) implementado
- [ ] IC80 avaliado em walk-forward
- [ ] Het gate aplicado
- [ ] Veredito honesto (GO ou KILL documentado)
- [ ] Suite verde

---

## Fase 5 — Live NWP

**Status:** NÃO INICIADO
**Regra:** Só após preditor ser escolhido (Fase 2+3)

### Implementação
- Live fetch de GFS/ECMWF single-run no CP
- Cache/snapshot
- Forecast live usando o mesmo caminho causal do backtest
- **Sem trading automático até shadow logs acumularem**

### Entregáveis
- ⏳ `core/ingest/nwp_live.py` (ou extensão do `nwp.py`)
- ⏳ Cache/snapshot manager
- ⏳ Integração com `forecast` CLI
- ⏳ Shadow logs

### Critério de done
- [ ] Fetch live funcional pra GFS e ECMWF
- [ ] Cache validado
- [ ] Forecast live bate com backtest no caminho causal
- [ ] Shadow logs acumulando
- [ ] **NÃO** ativa trading automático

---

## Ordem Recomendada (resumo)

1. **Fase 1 — Fixes do update.txt** (P1 + P2)
2. **Fase 2 — Rodar matriz consolidada**
3. **Fase 3 — `--model auto` + fix pyproject**
4. **Fase 4 — CQR** (gated, só após 1+2)
5. **Fase 5 — Live NWP** (após preditor)

**Ponto principal:** ECMWF não deve ser descartado pelo KILL do T-11-5. Falhou auto-promote de ponto, mas mostrou sinal forte em CP20-22. Rota certa = spread + decisão condicional, não vencedor global.

---

## O que NÃO fazer agora

- ❌ Implementar ideias do `Minimax_Ideias.md` (mixture-of-experts, B-ECE, etc.)
- ❌ Reabrir confidence/trading gate antes da Fase 5
- ❌ Auto-promote de preditor sem passar pelo matriz consolidada
- ❌ Mexer em contratos frozen (prereg, contracts/)

---

## Ideias do `Minimax_Ideias.md` (mantidas para análise futura)

Ver `D:\Downloads\Minimax_Ideias.md` (2026-06-01). Serão revisitadas APÓS o baseline estar implementado e validado:

1. Mixture-of-Experts ex-ante
2. B-ECE pivot (paradigm shift em REQ-AUD-5)
3. Kelly adaptativo com IC atual
4. Cross-station transfer learning
5. UMAP regime embedding
6. Climatologia ponderada por regime
7. Validar com o bookmaker (Brier Polymarket)

---

*Plano salvo em 2026-06-01. Sujeito a revisão conforme cada fase evolui.*
